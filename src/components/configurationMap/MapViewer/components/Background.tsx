import { CustomComponent } from './types';

/** 背景组件 */
const Background: CustomComponent = () => {
  return (
    <div style={{ width: '100%', height: '100%', background: '#fff' }} />
  );
}

export default Background;
