/** id自动生成时的前缀 */
export const defaultIdPrefix: string = 'node-';

/** 默认的zIndex */
export const defaultZIndex: number = 10;

/** 节点的最小尺寸 */
export const nodeMinSize = {
  width: 10,
  height: 10,
}

/** 背景组件的标识名称 */
export const backgroundNodeShape: string = 'background';

/** 背景组件的默认zIndex */
export const defaultBackgroundZIndex: number = 0;
